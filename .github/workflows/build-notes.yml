name: Build lecture notes

on:
  push:
    branches: [main]
    paths:
      - 'notes/**.md'
      - 'scripts/**'
      - 'shared/notes.css'
  workflow_dispatch:

# Shared with build-slides.yml. Both workflows commit and push to main, so they
# must not run at the same time or one of the two pushes gets rejected.
concurrency:
  group: publish-main
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pandoc
        run: |
          wget -q https://github.com/jgm/pandoc/releases/download/3.2/pandoc-3.2-1-amd64.deb
          sudo dpkg -i pandoc-3.2-1-amd64.deb

      - name: Convert markdown notes to HTML
        run: bash scripts/convert_notes.sh

      - name: Commit converted HTML
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add notes/*.html
          git diff --cached --quiet && exit 0
          git commit -m "auto: rebuild HTML notes from markdown"
          git push || (git pull --rebase && git push)
